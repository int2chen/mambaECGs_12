First, perform data preprocessing (data_pre_t3.py)
then run the main program (all_ptb_xl).
Place the three code files in the same directory. 
The environment requirements are as follows:
Python 3.10.18, PyTorch 2.8.0, CUDA 12.8, Mamba 2.2.2, 
The environment is compatible with NVIDIA 50-series GPUs.
